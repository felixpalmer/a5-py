# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Rust / PyO3 extension module
target/
*.so
*.pyd
*.dylib
# Local cargo overrides (e.g. patching the a5 crate to a working copy)
.cargo/config.toml

# cibuildwheel output
wheelhouse/

# Untracked scratch script for ad-hoc verification (see CLAUDE.md)
debug.sh
